# Phase 2 交付 · AI 爆款导演拆解报告（6 段式）

> 目标：把原先「爆款分析 + 剪辑拆解 + 节奏 + 标题 + 拍摄 + 精品化 + 复现 + 高级功能」一盘散沙的报告，收口成你 spec 里的 **6 段式《AI 爆款导演拆解报告》**，并新增「黄金3秒 / 情绪曲线 / 爆款公式」三块核心价值。

## 一、报告现在长这样（6 段）

| 段 | 名称 | 内容 | 门槛 |
|---|---|---|---|
| 第一部分 | 爆款评分（评分体系） | 综合评分环 + 4 维度 + 为什么火（成功原因）+ 提分目标 | **全部可见** |
| 第二部分 | 黄金3秒拆解 | 钩子类型 / 前 3 秒脚本 / 为什么能留人 / 3 条改造建议 | 付费 |
| 第三部分 | 视频结构拆解 | 时间轴结构拆解（钩子→铺垫→展开→高潮） | 付费 |
| 第四部分 | 情绪曲线 | 内联 SVG 情绪曲线图（6 个时间点）+ 走势说明 | 付费 |
| 第五部分 | 爆款公式提炼 | 一句话公式 + 4 因子权重条 | 付费 |
| 第六部分 | 可复制分析 | 可复制模板 + 复现路径 + 10 个标题 + 拍摄落地建议 | 付费 |

> 深拆 5 段（第二~六部分 + 精品化门槛）按定价矩阵走门禁：**免费档只给评分体系，其余 5 段显示「升级解锁」预览卡**；创作者/进阶/专业档全开。匿名用户先走登录门禁。

## 二、删掉的（对齐「去剪辑化」）

- 删除「智能剪辑」按钮（路由 `/studio` 在 Phase 1 已删，原按钮指向死链）。
- 删除独立的「特效拆解 / 节奏分析」段（剪辑向，已不符合定位；其数据仍生成供分镜/文案流程使用，只是不再在报告里单列）。
- 删除「高级功能·会员专属」整块（开头吸引力深度分析 / 标题优化器 / 选题推荐）—— 这些能力已变成免费可见的黄金3秒 / 标题段，再锁一层会自相矛盾。
- 清理死代码：`AdvancedModule` / `recommendTopics` / `Stat` 组件 + 未用图标导入。

## 三、新增数据模型（lib/types.ts）

```ts
Golden3s   { hookType, transcript, why, rebuild[] }
EmotionCurve { points: { tSec, level, label }[], note }
ViralFormula { formula, factors: { name, weight, tip }[] }
```

三块在 `AnalysisReport` 上为可选字段，向后兼容（`storyboard.ts` 依赖的 `pacing`/`structure` 等字段保留）。

## 四、数据来源

- **Mock 路径**（`lib/ai/mock.ts`）：`generateMockReport` 直接产出三块真实样例数据；`buildPrompt` 已扩展，让真实模型（DeepSeek/Qwen 等）也返回这三段。
- **真实模型路径**（`lib/ai/providers.ts`）：`RawReport` 类型扩展 + 清洗透传，未返回时自动降级 Mock（已验证：环境未配 qwen key 时优雅回退，报告正常生成）。
- **样例报告**（`lib/mock-data.ts` 的 `SAMPLE_REPORT`）：补齐三块字段，保证首页简版报告与 `/report?sample` 不缺数据。

## 五、情绪曲线图

纯内联 SVG（`EmotionCurveChart` 组件），无第三方图表依赖。用 `viewBox` 自适应宽度，标出峰值点，时间轴标注秒数。契合「依赖克制」原则。

## 六、验证

- `tsc --noEmit` 零类型错误；`npm run build` 通过（exit 0）。
- 运行时冒烟（生产服务 :3100）：
  - `GET /api/analyze?sample=1` → `golden3s`/`emotionCurve(6点)`/`formula(4因子)` 齐全。
  - `POST /api/analyze` → mock 正常产出三块新数据（综合分 87）。
  - `/`、`/report`、`/analyze`、`/pricing` 均 200，无运行时错误。

## 七、下一步（Phase 3+，待你拍板）

- **Phase 3**：案例库 → 爆款公式库（沉淀可复用公式 + 行业模板）。
- **Phase 4**：我的 AI 导演（用户档案字段 + 长期优化）。
- **Phase 5**：爆款复刻助手（按用户行业一键生成标题/脚本/分镜，新 API + 报告页入口）。
- **部署**：等你买国内轻量服务器（腾讯云/阿里云 ¥58/年，Ubuntu 22.04）后，我指导国内部署（Docker + 同机 PostgreSQL）。
